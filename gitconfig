[color]
    ui = auto
    diff = auto
    status = auto
    branch = auto
    interactive = auto
[gc]
    auto = 1
[user]
    name = Joe Heck
    email = heckj@mac.com
[alias]
    ci = commit
    st = status
    co = checkout
    oneline = log --prety=oneline
    br = branch
    la = log --pretty=\"format:%ad %h (%an): %s\" --date=short
    alog = log --graph --pretty=\"format:%ad %h (%an): %s\" --date=short
    review = push gerrit HEAD:refs/for/master
